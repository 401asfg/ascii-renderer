from setuptools import setup

setup(name='ascii_renderer',
      version='1.0.0',
      description='A toolset for rendering dynamic scenes as ascii characters',
      author='Michael Allan',
      packages=['ascii_renderer'],
      zip_safe=False)
